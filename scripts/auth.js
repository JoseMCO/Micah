// const _ = require('lodash');

// exports.regex = /micah auth/;
// // exports.example = 'micah <chuchada>: Le cuenta tu <secreto> al grupo.'

// exports.onMsg = function(bot, msg) {
//   const chatId = msg.chat.id;
//   const userId = msg.from.id;


//   if (!this.db.is_admin(userId)) {
//     bot.sendMessage(chatId, "No eres admin!");
//     this.db.set_admin(userId, true);
//     return false;
//   }
//   else {
//     this.db.set_admin(userId, false);
//     bot.sendMessage(chatId, "terrible admin!");
//   }
// };
